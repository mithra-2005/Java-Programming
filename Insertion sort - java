public class Main
{
	public static void main(String[] args) {
	       int[] arr = { 6 , 3 , 8 , 5 , 9};
	       int n = arr.length;
	       int i , j , key;
	       for(i = 1; i < n ; i++)
	       {
	           key = arr[i];
	           j = i - 1;
	           while( j >= 0 && arr[j] > key){
	           arr[j+1] = arr[j];
	           j--;
	           }
	       arr[j + 1] = key;
	       }
	       for(int X:arr)
	       System.out.print(X + " ");
	}
}
